<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= book.title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/edit-book.css" />
  </head>
  <body
    class="edit-body"
    aria-label="background image of books on wall with wooden ladder"
  >
    <%- include('../partials/_navbar.ejs') %>

    <div class="container">
      <div class="card">
        <h1>Edit <%= book.title %></h1>
        <% if (!user) { %>
        <a href="/auth/signup" class="btn btn-primary">Sign Up</a>
        <a href="/auth/signin" class="btn btn-secondary">Sign In</a>
        <% } %>

        <form action="/books/<%= book._id %>?_method=PUT" method="POST">
          <div class="form-group">
            <label for="title">Title:</label>
            <input
              type="text"
              id="title"
              name="title"
              value="<%= book.title %>"
              class="form-control"
              required
            />
          </div>
          <div class="form-group">
            <label for="author">Author:</label>
            <input
              type="text"
              id="author"
              name="author"
              value="<%= book.author %>"
              class="form-control"
              required
            />
          </div>
          <div class="form-group">
            <label for="description">Description:</label>
            <textarea
              id="description"
              name="description"
              rows="4"
              class="form-control"
            ><%= book.description %></textarea>
          </div>
          <div class="form-group">
            <label for="type">Genre:</label>
            <select id="type" name="type" class="form-control" required>
              <option value="Fiction" <%= book.type === 'Fiction' ? 'selected' : '' %>>Fiction</option>
              <option value="Non-Fiction" <%= book.type === 'Non-Fiction' ? 'selected' : '' %>>Non-Fiction</option>
              <option value="Mystery" <%= book.type === 'Mystery' ? 'selected' : '' %>>Mystery</option>
              <option value="Science Fiction" <%= book.type === 'Science Fiction' ? 'selected' : '' %>>Science Fiction</option>
              <option value="Fantasy" <%= book.type === 'Fantasy' ? 'selected' : '' %>>Fantasy</option>
              <option value="Biography" <%= book.type === 'Biography' ? 'selected' : '' %>>Biography</option>
              <option value="Novel" <%= book.type === 'Novel' ? 'selected' : '' %>>Novel</option>
              <option value="Short Story" <%= book.type === 'Short Story' ? 'selected' : '' %>>Short Story</option>
              <option value="Poem" <%= book.type === 'Poem' ? 'selected' : '' %>>Poem</option>
              <option value="Other" <%= book.type === 'Other' ? 'selected' : '' %>>Other</option>
            </select>
          </div>
          <button type="submit" class="btn btn-primary">Update Book</button>
        </form>
        <a href="/books/<%= book._id %>" class="btn btn-secondary"
          >Back to Book Details</a
        >
      </div>
    </div>
  </body>
</html>
